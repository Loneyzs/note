### 创建本地仓库

`git init`

`git remote add origin` 仓库地址



### 日常开发流程

`git pull`

`git add xxx/.`

`git commit -m ""`

`git push (origin xx分支)`

`git stash`

`git stash pop`



### 分支

`git branch` 查看分支

`git checkout (-b) xx`(创建并)切换分支

`git branch -d xx`删除分支

切换回master 后 `git merge xx` 合并分支



### 版本回退

`git log`

`git reset --hard commit_id`