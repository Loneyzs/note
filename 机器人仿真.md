# 建模

### 文件结构

* 创建单独的 description 功能包

* ```
  my_robot_description/
  ├── package.xml
  ├── CMakeLists.txt
  ├── urdf/  # 存放 URDF 或 xacro 模型文件
  │   ├── my_robot.urdf
  │   ├── my_robot.urdf.xacro
  │   ├── base.xacro
  │   ├── wheel.xacro
  │   ├── sensors.xacro
  │   └── materials.xacro
  ├── meshes/  # 存放三维模型文件，例如 stl、dae
  │   ├── base_link.stl
  │   ├── camera_link.stl
  │   └── arm_link.dae
  ├── launch/  # 存放启动文件
  │   ├── display.launch.py
  │   ├── rsp.launch.py
  │   └── gazebo.launch.py
  ├── rviz/    # 存放 RViz 配置
  │   └── display.rviz
  └── config/
      └── controllers.yaml
  ```

### URDF建模

**概念**

是机器人结构的唯一事实来源。

使用XML格式文件描述机器人几何结构、传感器、执行器等信息，主要描述

```python
1. link：机器人上的刚体部件 # 必须
2. joint：link 和 link 之间的连接关系 # 必须
3. visual：这个部件在 RViz 里长什么样 # 必须
4. collision：这个部件用于碰撞检测时的形状
5. inertial：这个部件的质量和惯量
6. transmission / ros2_control：控制接口相关内容
7. gazebo / mujoco 扩展标签：仿真器相关配置
```

**伪代码**

```xml
<robot name="my_robot">

  <link name="base_link">
    <visual>
      <geometry>
        <box size="0.5 0.3 0.1"/>
      </geometry>
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <material name="blue">
        <color rgba="0 0 1 1"/>
      </material>
    </visual>
  </link>

  <link name="camera_link">
    <visual>
      <geometry>
        <box size="0.05 0.05 0.05"/>
      </geometry>
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <material name="black">
        <color rgba="0 0 0 1"/>
      </material>
    </visual>
  </link>

  <joint name="base_to_camera" type="fixed">
    <parent link="base_link"/>
    <child link="camera_link"/>
    <origin xyz="0.2 0 0.15" rpy="0 0 0"/>
  </joint>

</robot>
```

### 使用Xacro简化URDF

模块化，便于复用

###  RViz 显示URDF模型

**链路**

```
URDF / xacro 文件
        ↓
robot_state/joint_publisher 工具节点读取模型
        ↓
把模型发布成 /robot_description 参数
        ↓
RViz 的 RobotModel 从 /robot_description 读取并显示
```

**工作流**

* `description_pkg/launch/` 写 `display.launch.py` 文件

  * 将模型目录作为参数添加到 `robot_state/joint_publisher` 工具
  * 启动 `robot_state/joint_publisher`、`rviz2` 节点

* 添加到 `setup.py` 

  * ```python
    import os
    from glob import glob
    
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
    
        # 安装 launch 文件
        (os.path.join('share', package_name, 'launch'), glob('launch/*.launch.py')),
    
        # 安装 urdf 文件
        (os.path.join('share', package_name, 'urdf'), glob('urdf/*.urdf')),
        
        (os.path.join('share', package_name, 'urdf'), glob('urdf/*.xacro')),
    ],
    ```

* ```bash
  # 运行启动文件
  colcon build
  source install/setup.bash
  ros2 launch my_robot_description display.launch.py
  ```

* RViz 中 Add → RobotModel，Description Topic 选择 `/robot_description`

* RViz 中 File → Save Config As 到功能包 `/rviz` 下

  * 此时可以在 launch 文件中加入 rviz 启动的参数，使用该配置文件目录

### 添加物理属性

待学习



# gazebo

### 用gazebo加载机器人模型

**工作流**

* 同用 RViz 显示的流程：写 launch 文件可直接启动 gazebo 节点（额外进行 urdf -> sdf 的转换）

**使用 gazebo 标签扩展 URDF 文件**

* 在 urdf 描述中添加给 gazebo 看的静态模型属性
* 也可添加控制插件，提供 gazebo 内物理仿真运动，与控制源码无关

### ros2_control

**介绍**

* 机器人控制的标准化框架，解耦上层控制算法和底层硬件驱动
* 对上层（控制器）：提供统一的接口标准
* 对底层（硬件/仿真）：通过插件形式适配不同的执行器或传感器，插件描述在 urdf 文件中
  * 仿真直接使用 gazebo 提供的 `gazebo_ros2_control` 插件
  * 实机自己写cpp底层硬件控制插件。纯做仿真可以不用下位机。







