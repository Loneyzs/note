* 每层都是一个经验函数 * 自己找的参数？
* 梯度是什么？为什么导数和找参数有关？梯度消失是什么？
  * 对损失函数求导会涉及到激活函数导数，是把loss当成所有的关于w和b的函数了吗？
  * 为什么是直接的贪心，这样出来不是局部最优解吗？
* 卷积是什么？卷积的本质是什么？
* 卷积神经网络还是每层多个节点吗？输入怎么变成多维度的？
* 输入的批次是什么含义？为什么每个批次都对应一层卷积核？（❌）



## 全连接神经网络

**前向传播**

* 神经元节点：上一层的数据线性加权，输入节点非线性激活函数，结果进入下一层。
  * 问题：线性 “一条” 输入，破坏图像原有信息

* 损失函数：
  * 多组x对应多组结果误差，描述多组误差大小的函数。
  * 嵌套多个激活函数的关于w和b的函数。

**反向传播**

* 梯度下降法：
  * 对损失函数w和b分别求导（本质是嵌套的激活函数的导数），沿梯度下降方向更新w和b。
  * 学习率：在更新参数时手动设置。
  * 多维空间极少有局部最优解，或随机梯度下降。

## 卷积神经网络

**卷积**

* 卷积运算：按卷积核大小划分等大区域，每个区域对应位置相乘相加
  * 步幅：卷积核每次滑动的距离
  * 填充：外围填充一圈0 。两者限定输出特征图大小
  * 本质：二维平面上通过卷积核加权平均，滑动出新的特征图输出
* 多通道卷积运算：每个通道对应卷积核相乘相加
  * 每个卷积核的通道数匹配输入，每个卷积核与输入对应通道相乘相加后输出单通道特征图
  * 卷积核个数决定卷积输出通道数，每个卷积核产生一个输出通道
  * 输入：长、宽、通道数；卷积核：层数（决定输出通道数）、通道数（与输入匹配）、长、宽

**池化**

* 最大池化运算：找每个区域的最大值
  * 步幅、填充：同理
  * 本质：操作，没有参数，不改变通道数，并不是神经网络里的层
* 平均池化运算：算每个区域的平均值

**结构 -- 前向传播**：

* 卷积、池化交替，每个卷积层的卷积核个数控制通道数
* 卷积网络部分输出多通道的提取后的特征值
* 最后连接全连接层，输出分类、回归的结果



## 经典卷积神经网络

**LeNet - 5** 

* 结构

  * 特征提取部分：卷积（sigmod激活函数）- 池化 - 卷积 - 池化

  * 全连接层（任务层）：3 * 全连接

**AlexNet**

* 结构：类似 LeNet, 5个卷积层（ReLu激活函数），3个全连接层
* Flatten操作：特征提取部分输出不满足全连接层输入时，强行展平，变为 `1*1*x` 输入到全连接层
* Dropout操作：防止过拟合
  * 每轮训练随机（临时）删除一些神经元节点，即不再往下进行输出
  * 前向传播后，反向传播时梯度下降法只更新没有删除的神经元的参数
* 数据集增强操作（水平翻转、随即裁切、PCA等）：一张数据集当成很多张用，且防止过拟合

**VGG**

* “块”结构

**GoogLeNet**

* Inception块
* 全局平均池化操作：单个通道内直接取平均，通常用于输入全连接层之前，代替Flatten层

**ResNet**

* 残差块：ResNet的基本块结构，每个块有主分支 和 捷径跨层连接通道
  * 梯度可以通过捷径顺畅方向传播，避免网络加深后梯度消失等训练问题
  * 主分支必要时可以控制为0, 保证每个残差块至少不会比原来更差：只训练需要的残差主分支
* 批量归一化（BN）层：







## 卷积神经网络实战

**项目结构**

```
cnn_project/
├── config/          # 配置模块：存放超参数与全局配置
├── utils/           # 工具模块：数据加载、图像预处理与自定义函数
├── data/            # 数据集
├── models/          # 模型本身，代码很简单
├── train.py         # 训练代码（模板，可复用）
└── evaluate.py      # 测试代码（模板，可复用）
```

**模型建立**：

* 初始化：用 pytorch 库封装好的过程（卷积、池化等）创建模型层
* 前向传播：接口传入数据，按照模型前向传播后给出输出

**模型训练**

1. 定义数据集（库封装好 / 自己）
2. 加载训练集，包括 `batch_size` 等参数
3. 定义损失函数反向传播等
4. 多轮训练